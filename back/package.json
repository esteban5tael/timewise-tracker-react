{
  "name": "timewise-tracker-backend",
  "version": "1.0.0",
  "description": "JSON Server backend for TimeWise Tracker",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "dev": "json-server --watch db.json --port 3001"
  },
  "keywords": ["json-server", "api"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
